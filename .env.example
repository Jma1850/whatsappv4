SUPABASE_URL="your-supabase-url"
SUPABASE_KEY="your-supabase-key"
OPENAI_API_KEY="your-openai-api-key"
GOOGLE_TTS_KEY="your-google-api-key"
TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
PORT="8080" # Optional: Port for the server to listen on, defaults to 8080

# Optional: Specify the OpenAI Whisper model for audio transcription.
# If set, this model is tried first. If it fails and is not 'whisper-1', 'whisper-1' is tried as a fallback.
# If not set, defaults to 'whisper-large-v3' as the first attempt.
# WHISPER_MODEL=whisper-1

# Optional: Specify the OpenAI model for translation.
# Defaults to "gpt-4o-mini" if not set.
# TRANSLATE_MODEL=gpt-4o-mini
